// This file previously registered its own 'hidetag' command (colliding
// with hidetag.js — undefined which one wins) and also claimed 'tagall'
// as an alias, colliding with the separate, visible-list tagall.js
// command. Its media-forwarding logic (the more complete of the two) is
// now the canonical implementation in hidetag.js.

module.exports = [];
